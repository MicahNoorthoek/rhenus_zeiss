$('.modal.in').modal('hide');
$('.dynamic-modal').html('<%= j render "lock_users" %>')
$('.dynamic-modal .modal').modal('show');
modalId = $('.dynamic-modal .modal').attr("id");
applySelect2(modalId)
