$('.modal.in').modal('hide');
$('.dynamic-modal').html('<%= j render "users/user_form", is_new_record: @user.new_record? %>')
$('.dynamic-modal .modal').modal('show');
modalId = $('.dynamic-modal .modal').attr("id");
applySelect2(modalId)